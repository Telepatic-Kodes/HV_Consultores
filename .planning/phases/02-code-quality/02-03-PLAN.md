---
phase: 02-code-quality
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
autonomous: true

must_haves:
  truths:
    - "@ts-nocheck removed from all component and page files"
    - "No dead code or orphaned files remain"
    - "Console statements cleaned up"
---

<objective>
Remove @ts-nocheck from components/pages, clean dead code, remove unused imports and console statements.

Purpose: Final code quality pass to ensure maintainable, type-safe codebase.
Output: Clean codebase with zero @ts-nocheck, minimal console.log, no dead code.
</objective>

<tasks>

<task type="auto">
  <name>Remove @ts-nocheck from component files</name>
  <action>
Remove @ts-nocheck from all 25 component files in src/components/.
Fix exposed type errors with proper interfaces.
  </action>
</task>

<task type="auto">
  <name>Remove @ts-nocheck from page files and remaining files</name>
  <action>
Remove @ts-nocheck from all page.tsx files, providers, hooks, and lib files.
Fix exposed type errors.
  </action>
</task>

<task type="auto">
  <name>Clean dead code, unused imports, console statements</name>
  <action>
- Remove commented-out code blocks
- Remove unused imports
- Replace console.log/error with structured approach or remove
- Delete orphaned files (e.g., Supabase types file if unused)
- Remove legacy Phase 7 docs if not needed
  </action>
</task>

</tasks>

# Plan 03-01: Landing Page Polish (Responsive, Favicon, Dead Links, Animations)

## Phase: 3 — Landing Page Polish
## Requirements: UXLP-01, UXLP-02, UXLP-03, UXLP-04, UXLP-05

## Goal
Landing page renders perfectly on mobile/tablet/desktop with working assets and no dead links.

## Pre-Conditions
- Phase 2 complete (zero TS errors, clean build)
- All landing components in `src/components/landing/`

## Assessment

| Requirement | Status | Work Needed |
|-------------|--------|-------------|
| UXLP-01 (Responsive) | Partial | Hero hidden on mobile, floating overflow |
| UXLP-02 (Hero stats) | ✅ DONE | Animated counters already show real values |
| UXLP-03 (Images/favicon) | ❌ Missing | No public/ dir, no favicon |
| UXLP-04 (Animations) | ✅ OK | tailwindcss-animate installed, animations defined |
| UXLP-05 (Dead links) | ❌ Broken | 7 dead # links in Footer |

## Tasks

### Task 1: Create favicon and public directory
- Create `public/` directory
- Generate an SVG favicon (HV initials in navy blue, matching brand)
- Create `src/app/favicon.ico` as SVG (Next.js 14 supports `icon.svg`)

### Task 2: Fix Hero responsive (mobile dashboard preview)
**File:** `src/components/landing/Hero.tsx`
- The dashboard preview is `hidden lg:block` — invisible on mobile/tablet
- Add a simplified mobile-friendly version visible on `lg:hidden` (show stat cards only)
- Fix floating elements: use `hidden xl:block` or constrain within bounds

### Task 3: Fix Footer dead links
**File:** `src/components/landing/Footer.tsx`
- Replace dead `#` links with real section anchors or remove them:
  - Nosotros → `#beneficios` (closest match)
  - Blog → remove (doesn't exist)
  - Carreras → remove (doesn't exist)
  - Contacto → `#contacto` ✅ already works
  - Privacidad → `/privacidad` (create stub page or anchor)
  - Términos → `/terminos` (create stub page or anchor)
  - LinkedIn → remove href or use `javascript:void(0)` with "Próximamente"
  - Twitter → same

### Task 4: Add smooth scroll behavior verification
- `html { scroll-smooth }` already in globals.css ✅
- Verify all anchor links have matching `id` attributes

### Task 5: Build verification
- `npx tsc --noEmit` → 0 errors
- `npm run build` → clean pass

## Success Criteria
1. Landing page displays correctly on mobile (375px), tablet (768px), desktop (1920px)
2. Hero stats show animated realistic values (170+, 95%, 50+)
3. Favicon displays in browser tab
4. All navigation links work (no 404s, no dead #)
5. Clean build passes

## Estimated Duration: ~30 min
